package com.newton.couplespace.ui.theme

import androidx.compose.ui.graphics.Color

// Romantic Color Palette for Bonded App

// Primary Colors - Pink Shades
val RomanticPink = Color(0xFFE91E63) // Primary
val RomanticPinkLight = Color(0xFFFCE4EC) // Light variant for containers
val RomanticPinkDark = Color(0xFFC2185B) // Dark variant for text on light containers

// Secondary Colors - Purple Shades
val RomanticPurple = Color(0xFF9C27B0) // Secondary
val RomanticPurpleLight = Color(0xFFF3E5F5) // Light variant for containers
val RomanticPurpleDark = Color(0xFF7B1FA2) // Dark variant for text on light containers

// Tertiary Colors - Red/Coral Shades
val RomanticRed = Color(0xFFFF5252) // Tertiary
val RomanticRedLight = Color(0xFFFFEBEE) // Light variant for containers
val RomanticRedDark = Color(0xFFD32F2F) // Dark variant for text on light containers

// Error Color
val ErrorRed = Color(0xFFB00020)

// Background and Surface Colors
val LightBackground = Color(0xFFFAF8FC) // Slightly off-white for light theme background
val LightSurface = Color(0xFFFFFFFF) // Pure white for light theme surfaces
val DarkBackground = Color(0xFF121212) // Dark gray for dark theme background
val DarkSurface = Color(0xFF1E1E1E) // Slightly lighter dark gray for dark theme surfaces